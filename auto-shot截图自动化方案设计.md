# auto-shot：HarmonyOS NEXT 语种资源截图自动化 —— 方案设计与难点分析

> 需求：给定语种资源的 `name`（或含该文案的截图），自动化找到展示它的页面；页面可滚动时把目标滚到视野内；截图保存为 `<name>.png` / `<name>.jpeg`。需覆盖"点击才触发"与"特定条件才显示"（如断网错误提示）的 UI。

---

## 一、需求拆解：这件事的本质

把需求拆成一条流水线：

```
输入 name（或截图）
  → ① 资源解析：name → 各语种文案值（string.json）
  → ② 页面定位：name → 它出现在哪个页面/组件（静态分析源码里的 $r 引用）
  → ③ 导航：从应用入口到达该页面
  → ④ 触发：让目标 UI 真正处于展示状态（直接可见 / 点击 / 条件满足）
  → ⑤ 滚动：目标滚入视野
  → ⑥ 截屏：校验后保存 <name>.png
```

**截屏本身是最简单的一步**（`hdc shell uitest screenCap -p ...` 或 `snapshot_display`，一条命令）。真正的难点在 **②页面定位的准确性、③导航的可达性、④条件 UI 的可控触发**。方案设计的重心应放在这三处。

---

## 二、总体架构：五层设计

```
┌─────────────────────────────────────────────────────────┐
│ E. 校验与产出层：视口命中校验 → OCR 复核 → 命名/去重/报告    │
├─────────────────────────────────────────────────────────┤
│ D. 触发与状态注入层（难点核心）                             │
│    L0 直接可见 → L1 脚本化点击 → L2 环境故障注入            │
│    → L3 白盒状态注入 → L4 自动探索兜底                     │
├─────────────────────────────────────────────────────────┤
│ C. 执行层：hdc + hmdriver2/Hypium                         │
│    导航、滚动到视野、idle 等待、截屏、全局弹窗处理            │
├─────────────────────────────────────────────────────────┤
│ B. 导航层：deeplink 直跳 > 场景脚本 > 自动探索 > 人工标注    │
├─────────────────────────────────────────────────────────┤
│ A. 资源与代码解析层（离线）                                 │
│    string.json 解析 / $r 引用扫描 / 触发方式分类 / 截图 OCR  │
└─────────────────────────────────────────────────────────┘
```

### A. 资源与代码解析层（离线跑，产出的"地图"质量决定整个系统的上限）

**范围约定（已对齐）**：只考虑纯 ArkUI 实现（不存在 Flutter/RN/H5 内嵌页）；资源引用需同时覆盖 `$r` 语法与 `resourceManager` API 两种方式。

1. **资源解析**：解析 `resources/{base, zh_CN, en_US, ...}/element/string.json`，建立 `key → {locale → value}` 映射；注意同名 key 在多模块（HAP/HAR/HSP）中可能冲突，命名产物时带模块前缀。
2. **静态分析方案（两阶段）**：

   **阶段一 regex MVP**（几天跑通端到端，覆盖字面量用法约 80-90%）：
   - `/\$r\(\s*['"]app\.string\.(\w+)['"]/`
   - `/getStringSync\(\s*\$r\('app\.string\.(\w+)'\)\.id/`
   - `/(getStringByNameSync|getStringByName|getPluralStringByNameSync|getStringArrayByNameSync)\(\s*['"](\w+)['"]/`

   **阶段二 TypeScript Compiler API AST 分析（正式方案）**。ArkTS 的 `struct` 关键字会导致 TS 解析失败，预处理一步做词法级替换 `struct Index` → `class Index`（`@Component`/`@Entry`/`@State` 等装饰器是合法 TS 语法无需处理），然后 `ts.createSourceFile` 正常解析。关键依据：**ArkTS 编译器要求 `$r()` 参数必须为字符串字面量（动态拼接无法编译），因此 `$r` 引用可 100% 静态解析；解析盲区仅存在于 resourceManager 按名 API 传变量的情况**。

   引用提取规则：

   | 规则 | 代码形态 | 解析方式 |
   |---|---|---|
   | R1 | `Text($r('app.string.key'))` | 直接提取字面量 |
   | R2 | `getStringSync($r('app.string.key').id)` | 解包 `.id` |
   | R3 | `getStringByNameSync('key')` 等按名 API | 提取字面量 |
   | R4 | `getStringByNameSync(this.keyName)` | 常量传播：沿作用域链找属性初始化器/顶层 const 的字面量 |
   | R5 | 业务封装 `I18n.t('login_title')` | 函数摘要：函数体内以"参数"调用 R3 API → 注册为 i18n 访问器并记录参数位，调用点对应位置字面量即 key |
   | R6 | `MAP[type].key` 查表 | 尝试解析 const 对象/Map；失败标记 dynamic（LLM 辅助 + 人工确认兜底） |

   AST 方案相对 regex 的必要性：区分注释/字符串中的伪引用、R4/R5 变量与封装追踪、上下文分类都需要语法树。

3. **上下文分类**（对每个 key 打标，决定走 D 层哪条路径）：沿 AST 祖先链判断——
   - `visible`：在 `build()`/`@Builder` 顶层且不在任何条件分支内直接渲染；
   - `conditional`：在 `if/else`、三元表达式内 → 同时记录**条件表达式源码**（如 `if (!this.networkOk)`）作为触发提示；
   - `click`：出现在 `AlertDialog.show`/`promptAction.showToast`/`showDialog`/`CustomDialog`/`bindSheet`/`bindContentCover` 内 → 顺藤摸瓜找触发按钮；
   - 运行时触发：在 `onClick` 等事件回调、`.catch`/异常分支内。
4. **导航图**：节点 = `@Entry` 页面（来自 `module.json5` pages / `route_map.json`）；边 = `router.pushUrl({url})`/`replaceUrl`、`NavPathStack.pushPath/pushPathByName`、module.json5 的 deeplink skills；普通组件沿 import 链归到引用它的页面。产出 `key → 候选页面集合（带最短导航路径）`。
5. **双向对账与校验**：提取出的 key 必须存在于 string.json（否则跨模块冲突或笔误）；string.json 零引用的 key 单列报告（未使用或 R6 动态引用）；用 50 个人工核对样本做金标集验证准确率。
6. **可选终极补洞**：debug 构建在资源加载层上报"当前页面实际渲染了哪些 key"（纯 ArkUI 下该层完全可控），用运行时数据修正静态结果。
7. **输入是截图时**：PC 端 OCR（PaddleOCR 等）提取文本 → 在当前语种 `string.json` 的 value 里做精确/模糊反查 → 得到候选 key（可能多个，需全部尝试或按使用频次排序）。这一步天然有歧义：一个显示文案可能对应多个 key。

### B. 导航层：怎么"到达"那个页面

按成本从低到高、确定性从高到低，四级策略：

| 优先级 | 策略 | 说明 |
|---|---|---|
| 1 | **Deeplink / 路由直跳** | 工程若用 Navigation/router 且注册了 URI，直接拉起目标页，最快最稳 |
| 2 | **场景脚本（Scene Registry）** | 维护 `key/页面 → 操作步骤` 的 YAML 注册表（人工/半自动生成），可复用、可回归 |
| 3 | **自动探索（爬虫式 BFS）** | dump 控件树建"屏幕图"，对可点节点逐一点击、返回、去重，直到到达目标页面；每次运行沉淀为新场景脚本 |
| 4 | **人工标注兜底** | 以上都失败的目标进"未命中清单"，人工补路径后入库 |

前置状态（登录、授权、首页数据）作为导航层的一部分处理：测试账号 + 首次引导自动跳过 + 登录态复用（避免每条 key 重复登录）。

### C. 执行层：滚动到视野与截屏

**滚动到视野（可靠做法是自己实现"滚→查→校→停"循环，不依赖框架是否提供 scrollIntoView）**：

```
loop（上限 N 次，防死循环）:
    tree = dumpLayout()                      # 无障碍树
    node = tree.find(text ≈ 目标文案)         # 精确匹配优先，降级为包含匹配
    if node 不存在:
        沿当前可滚动容器方向 swipe 一步(约 2/3 屏, 带加速度)   # 懒加载列表必须边滚边找
        wait_idle(); continue
    if node.bounds 完全在视口内 且 无遮挡:
        break
    else:
        根据 node.bounds 与视口的偏移量计算小步 swipe，使其居中
        wait_idle()
```

- `wait_idle()`：连续两次 dumpLayout 结果一致（或间隔 300ms 无变化）才继续，避免动画/加载中截图。
- 遮挡判断：吸顶栏、底部 tab、悬浮球可能盖住目标；可再微滚使其落在安全区。
- 匹配降级链：`text == value` → `text.contains(value)` → OCR 兜底（应对占位符格式化、富文本拼接导致的文本不一致）。

**截屏与命名**：

```bash
hdc shell uitest screenCap -p /data/local/tmp/shot.jpeg   # 或 snapshot_display -f
hdc file recv /data/local/tmp/shot.jpeg ./out/<name>.jpeg
```

⚠️ 已验证的坑：`snapshot_display`/`uitest screenCap` **实际输出是 JPEG**，与后缀名无关。需求要求 `<name>.png` 时必须在 PC 端用 Pillow 转格式，不能只改后缀。

**校验（截图前做，确定性最高）**：截图动作执行前，先用无障碍树确认目标节点 bounds 在视口内——这比截图后 OCR 便宜且可靠；OCR 作为可选的最终复核（防止"看起来在视口但被透明层遮挡"等边角情况）。

### D. 触发与状态注入层 —— 直接回答"点击才触发/断网才显示怎么办"

这是本需求的**最大难点**，没有银弹，正确做法是分层覆盖，逐层兜底：

| 层级 | 手段 | 覆盖场景 | 可靠性 | 成本 |
|---|---|---|---|---|
| **L0 直接可见** | 无需触发 | 静态文案、页面标题 | 高 | 零 |
| **L1 脚本化点击** | 静态分析得知文案在 Dialog/toast/popup 中 → 生成"进页 → 点触发按钮"脚本（LLM 辅助生成 + 人工确认一次，入 Scene Registry） | 确认框、菜单、弹窗、二级面板 | 高 | 一次性 |
| **L2 环境故障注入** | 网络类：让设备流量过可控代理（mitmproxy/whistle），按需对指定域名返回超时/断连/500/弱网——**不动系统网络，App 走到真实错误分支**；辅以飞行模式（控制中心 UI 自动化）模拟完全断网 | 断网提示、超时重试、5xx 错误页、弱网 loading 态 | 中高 | 中 |
| **L3 白盒状态注入（最推荐）** | 出 debug 构建包：① 目标页依赖的状态（如 `networkError=true`）通过 `AppStorage` 键 / debug 接口直接置位；② 做一个"预览页"，可按 key 直渲染任意组件并注入任意状态（错误/空态/加载中） | 所有条件 UI，包括无法从外部注入的状态（账号被封、余额不足、权限拒绝） | 极高 | 需要工程侧配合 |
| **L4 自动探索兜底** | 遍历当前页面可点节点，点击后检查目标是否出现，出现即截，随后返回 | 未知触发路径的长尾 | 低 | 低，但有风险 |

**关于 Toast/气泡这类"转瞬即逝"的 UI**：先准备"触发后立即连拍"——执行点击后以 200ms 间隔连拍 3~5 张，选目标 OCR 命中的那张；hmdriver2 自带 Toast 监听可辅助确认文本已出现。

**关于 L4 的安全性**（必须做）：自动点击等于"无差别乱点"，可能触发下单、支付、注销、删除。约束：只在测试账号 + 沙箱环境跑；维护按钮黑名单（文案/ID 含"支付、删除、退出登录、注销"等直接跳过）；破坏性操作白名单制。

**推荐组合**：L0~L2 覆盖 80% 场景；对确实无法从外部触发的条件 UI，推动工程侧加 L3（这是投入产出比最高的一步——ArkUI 是声明式的，"直接用错误态渲染组件"天然比黑盒模拟断网稳定得多）；L4 只作为发现未知路径的探索手段，产出脚本沉淀进 L1。

### E. 校验与产出层

- 产物：`<name>.png`（或 `.jpeg`），同名多模块加前缀；记录元数据（页面、语种、触发路径、时间）便于回归与排查。
- 未命中报告：导航失败 / 树中找不到 / 被遮挡 / 截图校验失败，分类输出，驱动 Scene Registry 迭代。
- 规模化：按页面聚合（导航一次，截完该页所有 key），多设备并行（hdc 天然支持多设备 farm），增量执行（只跑资源变更涉及的 key）。
- 多语言对比：同 key 不同语种截图可拼图/diff，服务翻译走查（这通常才是这类工具的业务价值所在）。

---

## 三、技术选型建议

| 组件 | 选型 | 理由 |
|---|---|---|
| UI 驱动 | **hmdriver2**（首选）/ Hypium | hmdriver2：无侵入、`pip install` 即用、控件树 dump、Toast 监听、uiautomator2 风格上手快；需要图像定位/分布式时再引入 Hypium |
| 设备通道 | hdc（Command Line Tools） | 安装/启停/截屏/文件收发，多设备 farm |
| 静态分析 | Python + regex MVP → TypeScript Compiler API（struct→class 预处理） | 已对齐：纯 ArkUI、覆盖 $r 与 resourceManager 两种引用；详见 A 层 |
| 故障注入 | mitmproxy / whistle 代理 | 断网/超时/5xx/弱网按需注入，不动系统 |
| OCR | PaddleOCR（PC 端） | 截图输入反查 key + 截图结果复核 |
| 场景注册表 | YAML 入库（git 管理） | 可评审、可回归、可由探索层自动沉淀 |
| 调度 | Python CLI + 多进程（按设备分片） | 后续再平台化 |

**迭代路线**：
1. **MVP（1~2 周）**：单设备单语种，A 层解析 + B 层人工路径表 + C 层滚动截图 + E 层校验，跑通 visible 类 key。
2. **二期**：L1 点击脚本 + L2 代理故障注入，覆盖弹窗与网络错误类；自动探索 L4 离线跑，沉淀场景。
3. **三期**：推动工程侧 debug 构建支持 L3 状态注入/预览页；多语种批量 + 多设备并行 + 报告平台化。

---

## 四、难点清单（按严重程度排序）

1. **条件 UI 的触发不可枚举（最大难点）**：断网、超时、空数据、权限拒绝、弱网、账号异常、版本过低……黑盒手段（L2）只能注入"环境可模拟"的条件；状态深藏在 ViewModel 里的只能靠白盒注入（L3）或 mock server。**纯黑盒方案在此注定不完整**，必须白盒/工程配合，这是要在立项时就向业务方讲清楚的预期。
2. **key → 页面映射的残余盲区**：范围已约定纯 ArkUI（无 H5/Flutter 内嵌），且 `$r` 因编译器要求字面量而可 100% 解析；残余盲区集中在 resourceManager 按名 API 传变量（R4/R6 解析失败）和服务端下发的文案。兜底：LLM 辅助 + 人工确认入场景注册表，或 debug 构建运行时上报渲染 key。
3. **懒加载列表的"滚动到视野"是盲搜**：无障碍树只包含已渲染的节点，没滚到的 item 根本不在树里，无法一次算出方向和距离，只能边滚边 dump；列表超长时耗时长且可能触发分页加载失败。另外吸顶/悬浮元素遮挡会让"在视口内"但实际被盖住。
4. **导航可达性/前置状态**：登录、实名、授权、引导页、千人千面的首页，都可能让"同一页面"在不同账号下长得不一样；需要专门的账号与状态管理设计。
5. **文案匹配歧义**：同一显示文案对应多个 key、多个页面出现同一文案、占位符格式化后（`%s`/`%d` 替换）与资源原始值不一致——匹配策略必须是"精确→包含→归一化→OCR"的降级链，且允许一对多时全截或按上下文消歧。
6. **多语言切换机制**：目标是"各语种目录"的截图，就要批量切语言。系统语言切换要么 UI 自动化设置页（慢、不稳），要么白盒用应用内语言设置（`i18n` 的应用级语言 API / AppStorage 注入），推荐后者；部分页面硬编码文案不随语言变，需在报告中标注。
7. **截屏权限与限制**：带 `SECURE` 标志的隐私页面（金融类常见）截屏会被系统拦截或输出黑图，黑盒无解，只能白盒渲染。
8. **稳定性**：控件树 dump 有延迟、页面动画未播完、系统级弹窗（升级提示、权限申请）打断流程——需要 idle 检测、全局弹窗处理器、失败重试与断点续跑。
9. **规模与效率**：数千 key × N 语种 × 导航开销，不聚合不并行根本跑不完；必须按页面聚合截屏 + 设备 farm + 增量执行。
10. **安全性**：自动点击可能误触支付/注销等破坏性操作，黑名单与沙箱账号是硬要求（见 L4）。

---

## 五、结论

- **可行**：以"hdc + hmdriver2 + 静态资源/代码解析 + 场景注册表"为骨架的黑盒方案可以稳定覆盖**直接可见 + 点击触发 + 网络类条件**的大多数 key，滚动与截屏环节技术成熟。
- **不现实的是追求纯黑盒 100% 覆盖**：状态型条件 UI、禁截屏页面、动态文案必须有工程侧配合（debug 状态注入 / 组件预览页 / mock server），建议把 L3 白盒能力作为二期与客户端团队共建的重点。
- **产品价值延伸**：同一套"资源 key → 截图"管线天然支持多语种对比与翻译走查报告，建议把产物元数据与 diff 能力从第一天就设计进去。
